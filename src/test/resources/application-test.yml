spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver

  mail:
    host: smtp.test.com
    port: 587
    username: test
    password: test
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.H2Dialect

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: dummy-client-id
            client-secret: dummy-client-secret
            redirect-uri: http://localhost:8080/login/oauth2/code/google

  cloud:
    aws:
      credentials:
        access-key: dummy-access-key
        secret-key: dummy-secret-key
      s3:
        bucket: dummy-bucket
        base-url: https://s3.dummy-url.com
      region:
        static: ap-northeast-2

jwt:
  secret: dGhpcy1pcy1hLXZlcnktbG9uZy1kdW1teS1zZWNyZXQta2V5LWZvci10ZXN0aW5nLXpurcG9zZS1kb250LXVzZS1pbi1wcm9kdWN0aW9uISEh

tmdb:
  api-key: dummy-tmdb-key
  base-url: https://api.themoviedb.org/3

youtube:
  api:
    key: dummy-youtube-key

grpc:
  client:
    service-a:
      address: static://localhost:9090
      negotiation-type: plaintext

ai:
  spoiler:
    url: http://localhost:8081
  refinement:
    url: http://localhost:5000
  recommendation:
    url: http://localhost:8082

app:
  auth:
    api-key: "dummy-api-key-for-test"